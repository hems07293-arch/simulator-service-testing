spring:
  application:
    name: simulator-service-testing
  datasource:
    url: jdbc:mysql://localhost:3306/simulator_service
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.tool.schema: DEBUG

server:
  port: 9010
